#include <stdlib.h>
#include <stdio.h>

int main()
{
	float b,h,area;
	printf("Input Triangle's base:");
	scanf("%f",&b);
	printf("Input Triangle's height:");
	scanf("%f",&h);
	area = b*h;
	printf("%1f \n", area);
	system("PAUSE");
	
}
